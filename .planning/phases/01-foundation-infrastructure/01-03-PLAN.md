---
phase: 01-foundation-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/admin/package.json
  - apps/admin/tsconfig.json
  - apps/admin/vite.config.ts
  - apps/admin/index.html
  - apps/admin/src/main.tsx
  - apps/admin/src/App.tsx
  - apps/admin/src/vite-env.d.ts
  - apps/portal/package.json
  - apps/portal/tsconfig.json
  - apps/portal/src/index.ts
  - .prettierrc
  - .prettierignore
  - .eslintrc.js
autonomous: true
requirements:
  - R1.1

must_haves:
  truths:
    - "React Admin app starts on port 5173 with Vite dev server and renders a shell page"
    - "React Portal package exists as a stub with package.json (not runnable yet)"
    - "Prettier formats all TypeScript/JSON files consistently"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "React Admin portal package configuration"
      contains: "@airport-revenue/admin"
    - path: "apps/admin/src/App.tsx"
      provides: "Admin portal root component"
      min_lines: 10
    - path: "apps/admin/vite.config.ts"
      provides: "Vite configuration with React plugin"
      contains: "react"
    - path: "apps/portal/package.json"
      provides: "Tenant portal stub package"
      contains: "@airport-revenue/portal"
    - path: ".prettierrc"
      provides: "Code formatting configuration"
      contains: "singleQuote"
  key_links:
    - from: "apps/admin/package.json"
      to: "packages/shared-types"
      via: "workspace dependency"
      pattern: "@airport-revenue/shared-types"
---

<objective>
Create the React Admin portal shell with Vite, the React Portal stub package, and project-wide code quality tooling (Prettier, ESLint base config). This completes R1.1 (all monorepo packages exist) and establishes DX standards.

Purpose: The admin and portal packages complete the monorepo workspace structure defined in R1.1. Code quality tooling prevents style drift from the start.
Output: Running React Admin shell at localhost:5173, portal stub, Prettier/ESLint configs.
</objective>

<execution_context>
@/Users/aslan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aslan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: React Admin shell with Vite and Portal stub package</name>
  <files>
    apps/admin/package.json,
    apps/admin/tsconfig.json,
    apps/admin/vite.config.ts,
    apps/admin/index.html,
    apps/admin/src/main.tsx,
    apps/admin/src/App.tsx,
    apps/admin/src/vite-env.d.ts,
    apps/portal/package.json,
    apps/portal/tsconfig.json,
    apps/portal/src/index.ts
  </files>
  <action>
    1. **apps/admin/package.json**: name "@airport-revenue/admin", private: true, type: "module". Scripts: dev (vite), build (tsc && vite build), preview (vite preview), lint (eslint src --ext ts,tsx). Dependencies: react@^18, react-dom@^18, react-router-dom@^6, @airport-revenue/shared-types (workspace:*). DevDependencies: @types/react@^18, @types/react-dom@^18, @vitejs/plugin-react@^4, vite@^5, typescript@^5.

    2. **apps/admin/tsconfig.json**: Extend ../../packages/tsconfig/react.json. compilerOptions: outDir ./dist, paths for @shared-types pointing to ../../packages/shared-types/src. Include: ["src"].

    3. **apps/admin/vite.config.ts**: Import react plugin from @vitejs/plugin-react. Export defineConfig with plugins: [react()], server: { port: 5173, proxy: { '/api': { target: 'http://localhost:3000', changeOrigin: true } } }, resolve: { alias: { '@shared-types': path.resolve(__dirname, '../../packages/shared-types/src') } }.

    4. **apps/admin/index.html**: Standard Vite HTML template with root div and script pointing to /src/main.tsx. Title: "Airport Revenue - Admin Portal".

    5. **apps/admin/src/main.tsx**: Import React, ReactDOM, App. Render App into root element with StrictMode.

    6. **apps/admin/src/App.tsx**: Simple shell component:
       - Display "Airport Revenue Management" as h1
       - Display "Admin Portal" as subtitle
       - Display "Phase 1 - Foundation Complete" as status text
       - Import and display UserRole enum values from @airport-revenue/shared-types to verify workspace linking works
       - Style with minimal inline styles (gray background, centered content, white card)

    7. **apps/admin/src/vite-env.d.ts**: Standard Vite client types reference.

    8. **apps/portal/package.json**: name "@airport-revenue/portal", private: true. Scripts: dev ("echo 'Portal not implemented yet - Phase 7+'"), build ("echo 'Portal not implemented yet'"). Dependencies: @airport-revenue/shared-types (workspace:*). Minimal — this is a stub.

    9. **apps/portal/tsconfig.json**: Extend ../../packages/tsconfig/react.json. Include: ["src"].

    10. **apps/portal/src/index.ts**: Export a placeholder: export const portal = { status: 'stub', message: 'Tenant portal - to be implemented in Phase 7+' };

    After creating files, run pnpm install from root to resolve new workspace packages.
  </action>
  <verify>
    <automated>cd /Users/aslan/Documents/Non-Aero && pnpm install && pnpm --filter @airport-revenue/admin exec -- tsc --noEmit && echo "ADMIN BUILD CHECK OK"</automated>
  </verify>
  <done>
    - apps/admin TypeScript compiles without errors
    - apps/admin can import and use enums from @airport-revenue/shared-types
    - apps/portal exists as stub package with package.json
    - pnpm workspace resolves all packages including admin and portal
  </done>
</task>

<task type="auto">
  <name>Task 2: Prettier and ESLint configuration for code quality</name>
  <files>
    .prettierrc,
    .prettierignore,
    .eslintrc.js
  </files>
  <action>
    1. **.prettierrc**: JSON config with:
       - singleQuote: true
       - trailingComma: "all"
       - printWidth: 100
       - tabWidth: 2
       - semi: true
       - bracketSpacing: true
       - arrowParens: "always"
       - endOfLine: "lf"

    2. **.prettierignore**: Ignore node_modules, dist, .turbo, coverage, pnpm-lock.yaml, apps/api/prisma/migrations.

    3. **.eslintrc.js**: Root ESLint config. Module.exports with root: true, parser: "@typescript-eslint/parser", plugins: ["@typescript-eslint"], extends: ["eslint:recommended", "plugin:@typescript-eslint/recommended"]. Rules: "@typescript-eslint/no-unused-vars": ["warn", { argsIgnorePattern: "^_" }], "@typescript-eslint/explicit-function-return-type": "off", "@typescript-eslint/no-explicit-any": "warn". Ignore patterns: ["node_modules", "dist", ".turbo", "coverage"].

    4. Install dev dependencies at root level:
       - pnpm add -Dw prettier eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin

    5. Add root package.json scripts:
       - "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
       - "format:check": "prettier --check \"**/*.{ts,tsx,json,md}\""
       - "lint": "turbo lint"
  </action>
  <verify>
    <automated>cd /Users/aslan/Documents/Non-Aero && pnpm format:check 2>&1 | head -20 && echo "PRETTIER CHECK DONE"</automated>
  </verify>
  <done>
    - Prettier config exists and can check all files
    - ESLint config exists with TypeScript support
    - Root package.json has format and lint scripts
    - Code formatting is consistent across all packages
  </done>
</task>

</tasks>

<verification>
1. `pnpm install` resolves all workspace packages (api, admin, portal, shared-types, formula-engine)
2. Admin app TypeScript compiles without errors
3. Portal stub package exists with package.json
4. `pnpm format:check` runs across all files
5. All 5 workspace packages listed in pnpm workspace
</verification>

<success_criteria>
- Monorepo has all 5 workspace packages: api, admin, portal, shared-types, formula-engine (R1.1 complete)
- React Admin shell compiles and can import shared-types
- Portal exists as stub package
- Prettier and ESLint configs enforce consistent code style
- All packages resolve via pnpm workspace
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md`
</output>
