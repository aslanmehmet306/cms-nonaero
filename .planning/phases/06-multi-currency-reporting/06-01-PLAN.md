---
phase: 06-multi-currency-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/exchange-rates/exchange-rates.service.ts
  - apps/api/src/exchange-rates/exchange-rates.service.spec.ts
  - apps/api/src/exchange-rates/exchange-rates.controller.ts
  - apps/api/src/exchange-rates/exchange-rates.module.ts
  - apps/api/src/exchange-rates/dto/create-exchange-rate.dto.ts
  - apps/api/src/exchange-rates/dto/update-exchange-rate.dto.ts
  - apps/api/src/exchange-rates/dto/query-exchange-rates.dto.ts
autonomous: true
requirements:
  - R10.2
  - R10.4

must_haves:
  truths:
    - "Admin can create an exchange rate with from/to currency, rate, effective date, and source"
    - "Admin can list exchange rates filtered by currency pair and date range"
    - "System finds the most recent exchange rate on or before a given effective date for a currency pair"
    - "Same-currency lookup returns rate 1.0 without database query"
    - "Duplicate rate entry (same pair + date + source) is rejected with unique constraint"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "ExchangeRate model with @@unique and @@index"
      contains: "model ExchangeRate"
    - path: "apps/api/src/exchange-rates/exchange-rates.service.ts"
      provides: "CRUD + getRate() effective date lookup + convert() helper"
      exports: ["ExchangeRatesService"]
      min_lines: 80
    - path: "apps/api/src/exchange-rates/exchange-rates.controller.ts"
      provides: "REST endpoints for exchange rate management"
      exports: ["ExchangeRatesController"]
      min_lines: 40
    - path: "apps/api/src/exchange-rates/exchange-rates.service.spec.ts"
      provides: "Unit tests for rate lookup, CRUD, edge cases"
      min_lines: 60
  key_links:
    - from: "apps/api/src/exchange-rates/exchange-rates.service.ts"
      to: "prisma.exchangeRate"
      via: "findFirst with effectiveDate lte + orderBy desc"
      pattern: "prisma\\.exchangeRate\\.findFirst"
    - from: "apps/api/src/exchange-rates/exchange-rates.service.ts"
      to: "apps/api/src/common/utils/decimal-helper.ts"
      via: "DecimalHelper.multiply for currency conversion"
      pattern: "DecimalHelper\\.multiply"
---

<objective>
Create the ExchangeRate Prisma model and ExchangeRatesModule with CRUD endpoints, effective-date rate lookup service, and currency conversion helper.

Purpose: Provides the foundational exchange rate infrastructure that the reporting module (Plan 03) will use for display-only currency conversion at period-end rates. This is the only new Prisma model in Phase 6.

Output: ExchangeRate schema model, ExchangeRatesService with getRate()/convert(), REST controller, unit tests.
</objective>

<execution_context>
@/Users/aslan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aslan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-currency-reporting/06-RESEARCH.md

@apps/api/prisma/schema.prisma
@apps/api/src/common/utils/decimal-helper.ts
@apps/api/src/app.module.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/api/src/common/utils/decimal-helper.ts:
```typescript
export class DecimalHelper {
  static multiply(a: Decimal.Value, b: Decimal.Value): Decimal;
  static roundMoney(value: Decimal.Value): Decimal;
  static isZero(value: Decimal.Value): boolean;
}
```

From apps/api/src/audit/dto/query-audit.dto.ts (pagination pattern):
```typescript
export class QueryAuditDto {
  @IsOptional() @Type(() => Number) @IsInt() @Min(1) page?: number = 1;
  @IsOptional() @Type(() => Number) @IsInt() @Min(1) perPage?: number = 25;
}
```

From apps/api/src/obligations/obligations.controller.ts (controller pattern):
```typescript
@ApiTags('...')
@ApiBearerAuth()
@Controller('...')
export class XxxController {
  @Get() @Roles(...) @ApiOperation({...}) @ApiResponse({...})
  findAll(@Query() query: QueryDto) { ... }
}
```

From @shared-types/enums (role references):
```typescript
export enum UserRole {
  super_admin, airport_admin, commercial_manager, finance, auditor, tenant_admin, tenant_user
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExchangeRate Prisma model + service with rate lookup and conversion</name>
  <files>
    apps/api/prisma/schema.prisma,
    apps/api/src/exchange-rates/exchange-rates.service.ts,
    apps/api/src/exchange-rates/exchange-rates.service.spec.ts,
    apps/api/src/exchange-rates/dto/create-exchange-rate.dto.ts,
    apps/api/src/exchange-rates/dto/update-exchange-rate.dto.ts,
    apps/api/src/exchange-rates/dto/query-exchange-rates.dto.ts
  </files>
  <behavior>
    - Test: create() stores exchange rate with all fields (fromCurrency, toCurrency, rate, effectiveDate, source, notes, createdBy)
    - Test: getRate('EUR', 'TRY', date) returns most recent rate on or before the date (not exact match)
    - Test: getRate('TRY', 'TRY', anyDate) returns rate=1.0 and source='identity' without DB query
    - Test: getRate('EUR', 'TRY', date) throws NotFoundException when no rate exists before that date
    - Test: convert(100, 'EUR', 'TRY', date) returns amount * rate, rounded to 2 decimal places
    - Test: convert(100, 'TRY', 'TRY', date) returns 100 unchanged (same currency identity)
    - Test: findAll with currency pair filter returns only matching rates
    - Test: create duplicate (same pair + date + source) throws unique constraint error
  </behavior>
  <action>
    1. Add ExchangeRate model to prisma/schema.prisma (after AuditLog section):
       - id: String @id @default(uuid())
       - airportId: String @map("airport_id")
       - fromCurrency: String @map("from_currency") (e.g., EUR, USD)
       - toCurrency: String @map("to_currency") (e.g., TRY)
       - rate: Decimal @db.Decimal(19, 8) (high precision for FX)
       - effectiveDate: DateTime @map("effective_date") @db.Date
       - source: String (MANUAL, TCMB, ECB)
       - notes: String?
       - createdBy: String @map("created_by")
       - createdAt: DateTime @default(now()) @map("created_at")
       - @@unique([fromCurrency, toCurrency, effectiveDate, source])
       - @@index([fromCurrency, toCurrency, effectiveDate])
       - @@map("exchange_rate")
       Do NOT add a relation to Airport (keep it simple, airportId is just a filter field).

    2. Create DTOs following existing project patterns:
       - CreateExchangeRateDto: airportId (UUID), fromCurrency (string, IsNotEmpty), toCurrency (string, IsNotEmpty), rate (number, IsPositive -- will convert to Decimal), effectiveDate (ISO8601 date string), source (string, IsNotEmpty), notes (optional string), createdBy (string, IsNotEmpty)
       - UpdateExchangeRateDto: rate (optional), notes (optional), source (optional) -- only mutable fields
       - QueryExchangeRatesDto: airportId (required UUID), fromCurrency (optional), toCurrency (optional), dateFrom (optional ISO), dateTo (optional ISO), page (default 1), perPage (default 25)

    3. Create ExchangeRatesService with:
       - create(dto): Creates exchange rate record. Use Prisma create. Rate stored as-is (Decimal column handles precision).
       - update(id, dto): Updates mutable fields only (rate, notes, source).
       - findAll(query): Paginated list with optional filters on currency pair and date range. Return { data, meta: { page, perPage, total, totalPages } }.
       - findOne(id): Find by ID, throw NotFoundException if missing.
       - remove(id): Delete by ID, throw NotFoundException if missing.
       - getRate(fromCurrency, toCurrency, effectiveDate): Core lookup method.
         * If fromCurrency === toCurrency, return { rate: new Decimal(1), source: 'identity', effectiveDate } immediately.
         * Otherwise: prisma.exchangeRate.findFirst({ where: { fromCurrency, toCurrency, effectiveDate: { lte: effectiveDate } }, orderBy: { effectiveDate: 'desc' } }).
         * Throw NotFoundException with descriptive message if no rate found.
         * Return { rate: new Decimal(exchangeRate.rate.toString()), source, effectiveDate }.
       - convert(amount, fromCurrency, toCurrency, effectiveDate): Convenience method.
         * Calls getRate(), then DecimalHelper.roundMoney(DecimalHelper.multiply(amount, rate)).
         * Returns { convertedAmount, rate, rateDate }.

    4. Create unit tests mocking PrismaService. Test all behaviors listed above. Follow the project pattern of using jest.fn() mocks for prisma methods. Use `new Decimal('35.12')` for rate values in tests.
  </action>
  <verify>
    <automated>cd /Users/aslan/Documents/Non-Aero/.claude/worktrees/quirky-sanderson && npx jest --testPathPattern="exchange-rates" --no-coverage --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    ExchangeRate model exists in schema.prisma with unique constraint and index. ExchangeRatesService has create/update/findAll/findOne/remove/getRate/convert methods. All 8+ unit tests pass. getRate returns most-recent-on-or-before rate. Same-currency returns identity rate without DB call. convert uses DecimalHelper for precision.
  </done>
</task>

<task type="auto">
  <name>Task 2: ExchangeRate controller + module + AppModule registration</name>
  <files>
    apps/api/src/exchange-rates/exchange-rates.controller.ts,
    apps/api/src/exchange-rates/exchange-rates.module.ts,
    apps/api/src/app.module.ts
  </files>
  <action>
    1. Create ExchangeRatesController following existing controller patterns (see obligations.controller.ts):
       - @ApiTags('Exchange Rates'), @ApiBearerAuth(), @Controller('exchange-rates')
       - POST / : Create exchange rate. @Roles(finance, airport_admin, super_admin). @Audit('ExchangeRate'). Body: CreateExchangeRateDto. Returns 201.
       - GET / : List exchange rates. @Roles(finance, airport_admin, super_admin, auditor, commercial_manager). Query: QueryExchangeRatesDto.
       - GET /:id : Get single rate. @Roles(finance, airport_admin, super_admin, auditor). Param: ParseUUIDPipe.
       - PATCH /:id : Update rate. @Roles(finance, airport_admin, super_admin). @Audit('ExchangeRate'). Body: UpdateExchangeRateDto.
       - DELETE /:id : Remove rate. @Roles(airport_admin, super_admin). @Audit('ExchangeRate').
       - GET /lookup : Rate lookup endpoint. @Roles(finance, airport_admin, super_admin, auditor, commercial_manager). Query params: fromCurrency (string), toCurrency (string), effectiveDate (ISO string). Calls service.getRate() and returns the rate object.

    2. Create ExchangeRatesModule:
       - imports: [] (DatabaseModule is global)
       - controllers: [ExchangeRatesController]
       - providers: [ExchangeRatesService]
       - exports: [ExchangeRatesService] (needed by ReportsModule in Plan 03)

    3. Register ExchangeRatesModule in AppModule imports array after NotificationsModule, with comment "// Phase 6: Multi-Currency & Reporting".

    4. Add all Swagger decorators: @ApiOperation, @ApiResponse for each endpoint with proper descriptions.
  </action>
  <verify>
    <automated>cd /Users/aslan/Documents/Non-Aero/.claude/worktrees/quirky-sanderson && npx jest --testPathPattern="exchange-rates" --no-coverage --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    ExchangeRatesController has 6 endpoints (CRUD + lookup). Module exports ExchangeRatesService. AppModule imports ExchangeRatesModule. All existing tests still pass. Swagger decorators present on all endpoints.
  </done>
</task>

</tasks>

<verification>
- ExchangeRate model in schema.prisma has @@unique([fromCurrency, toCurrency, effectiveDate, source])
- ExchangeRatesService.getRate() returns identity for same currency, most-recent-before for cross-currency
- ExchangeRatesService.convert() uses DecimalHelper.multiply + roundMoney (no native JS float math)
- All exchange-rate tests pass
- No regressions in existing test suite
</verification>

<success_criteria>
- ExchangeRate Prisma model added with proper Decimal(19,8) precision
- CRUD endpoints at /exchange-rates work with proper role guards
- Rate lookup at GET /exchange-rates/lookup returns effective rate for date
- Currency conversion helper available for Plan 03 (ReportsModule)
- All new + existing tests pass (339+ existing, new exchange-rate tests)
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-currency-reporting/06-01-SUMMARY.md`
</output>
